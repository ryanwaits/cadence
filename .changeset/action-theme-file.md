---
"@waits/cadence": patch
---

fix(action): forward --theme-file through the repo→render path (the Action's theme-file input previously no-oped on the MP4); add an `upload: true` input that attaches the MP4 to the triggering release.
