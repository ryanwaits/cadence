---
"@waits/cadence": minor
---

Add a beat-level `note` field — a handwritten flourish (theme's `fonts.note`, marker color) rendered under the headline on title/center/hero beats. Enables a logo-lockup closer like `{ layout: "center", headline: "Secondlayer", note: "Streams" }`.
